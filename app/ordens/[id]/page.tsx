"use client";
import {useState} from "react"; import {useRouter} from "next/navigation"; import Header from "@/components/Header"; import Card,{Barra} from "@/components/Card"; import StatusBadge from "@/components/StatusBadge"; import Modal from "@/components/Modal"; import {useData} from "@/components/DataProvider"; import {data,percentual} from "@/lib/utils";
export default function DetalheOrdem({params}:{params:{id:string}}){const router=useRouter();const {orders,addProduction,updateOrder}=useData();const base=orders.find(o=>o.id===Number(params.id));const [modal,setModal]=useState(false);const [qtd,setQtd]=useState("");if(!base)return <><Header titulo="OP não encontrada"/><div className="p-6"><button onClick={()=>router.push("/ordens")} className="text-sinal">← Voltar</button></div></>;const pct=percentual(base.produzido,base.quantidade);function apontar(){const n=Number(qtd);if(n>0)addProduction(base.id,n);setQtd("");setModal(false)}return <><Header titulo={`OP #${base.id}`} descricao={base.produto} acao={<StatusBadge valor={base.status}/>}/><div className="grid gap-6 p-6 lg:grid-cols-2"><Card titulo="Dados da ordem"><div className="space-y-2">{[["Produto",base.produto],["Código",base.codigo],["Quantidade",base.quantidade],["Produzido",base.produzido],["Responsável",base.responsavel],["Máquina",base.maquina],["Início",data(base.inicio)],["Entrega",data(base.entrega)]].map(([a,b])=><div key={String(a)} className="flex justify-between gap-4 border-b border-risco/60 py-2 text-sm"><span className="text-apagado">{a}</span><span className="font-medium">{b}</span></div>)}</div></Card><Card titulo="Andamento"><div className="flex items-end justify-between"><span className="num text-4xl font-bold">{pct}%</span><span className="num text-sm text-apagado">{base.produzido} / {base.quantidade}</span></div><div className="mt-4"><Barra percent={pct}/></div><div className="mt-6 flex flex-wrap gap-2"><button onClick={()=>updateOrder(base.id,{status:"EM_PRODUCAO"})} className="rounded-lg bg-ok/10 px-3 py-2 text-sm font-semibold text-ok">Iniciar</button><button onClick={()=>updateOrder(base.id,{status:"PAUSADA"})} className="rounded-lg bg-atencao/10 px-3 py-2 text-sm font-semibold text-atencao">Pausar</button><button onClick={()=>updateOrder(base.id,{status:"FINALIZADA"})} className="rounded-lg bg-slate-100 px-3 py-2 text-sm font-semibold">Finalizar</button><button onClick={()=>setModal(true)} className="rounded-lg bg-sinal px-3 py-2 text-sm font-semibold text-white">Apontar produção</button></div></Card></div><Modal titulo="Apontar produção" aberto={modal} aoFechar={()=>setModal(false)}><label className="block text-sm font-medium">Peças produzidas<input autoFocus type="number" min="1" value={qtd} onChange={e=>setQtd(e.target.value)} className="mt-1.5 w-full rounded-lg border border-risco px-3 py-2.5"/></label><button onClick={apontar} className="mt-4 w-full rounded-lg bg-sinal py-2.5 font-semibold text-white">Registrar</button></Modal></>}
